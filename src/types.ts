export type AuthDataSplited = {
    token_type?: string
    access_token?: string
    refresh_token?: string
}
export type AuthData = {
    tokenType?: string
    accessToken?: string
    refreshToken?: string
}